package Interview;

public class StackAndHeap {

    /*
     * We have stack memory and there is also heap memory.
     * We will store data and 10 in the stack, since stack consists of two columns.
     * Now for calling another method we will have different stack.
     * Objects are stored in heap memory and the address is stored in stack.
     */
    public static void main(String[] args) {
        int data = 10;
    }
}
